# Google Calendar API — required for the site to show events.
# Set these in Vercel's Environment Variables (Production + Preview),
# not in this file. This file is just documentation of what's needed.

GOOGLE_CALENDAR_ID=
GOOGLE_CALENDAR_API_KEY=
